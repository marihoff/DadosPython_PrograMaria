## Featuring Engeneerging
## Ex: preparar os ingredientes antes de cozinhar   

def preencher_nivel(gestor,nivel):
    if gestor==1:
        return 'Pessoa Gestora'
    else:
        return mivel
    
    dados.apply(lambda x: preencher_nivel(x['gestor'], x['nivel']), axis=1) #aplicar a função para preencher a coluna de nível com base na coluna de gestor

    

